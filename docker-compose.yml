services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: duys
      POSTGRES_PASSWORD: duys
      POSTGRES_DB: duys
    ports:
      - "5432:5432"
    volumes:
      - duys_pg:/var/lib/postgresql/data

volumes:
  duys_pg:
